const MAXWEAPS  = 2; 	  // Maximum weapons.
const NOWEAPSPR = "UNKN"; // Use this sprite when none is found for a dropped weapon.

// Pickup configuration.
const PICKUP_FOV	 = 20; // The FOV that the pickup must be visible with-in.
const PICKUP_RANGE	 = 64; // The maximum range a weapon can be picked up from.

// Strings to print when looking at/standing over weapon.
const PICKUP_STR1    = "\c[Black]$WEAPNAME";
const PICKUP_STR2    = "\c[BLUE]Hold \c[CYAN]\"USE\" \c[BLUE]to \c[CYAN]Pickup";